<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jxd.smsnew.dao.ITeacherDao">

    <select id="selectTeachersByPage" resultType="com.jxd.smsnew.vo.TeacherVO">
        SELECT
        t.teacher_id,
        t.teacher_name,
        t.sex,
        t.birthday,
        t.degree,
        t.title,
        t.hire_date,
        t.college_id,
        c.college_name
        FROM teacher t
        LEFT JOIN college c ON t.college_id = c.college_id
        <where>
            <if test="teacherName != null and teacherName != ''">
                t.teacher_name LIKE CONCAT('%', #{teacherName}, '%')
            </if>
            <if test="collegeId != null">
                AND t.college_id = #{collegeId}
            </if>
        </where>
    </select>
</mapper>